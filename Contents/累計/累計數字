//設計一個可以連續輸入個位數的程式(當輸入為0時程式中止)，算出哪幾個個位數字出現最多次，將這些數字和出現次數印出(2個數字以上時，將較小的數字排在前面)。
#include<stdio.h>  
int main(void)  
{  
    int a[10]={0};  
    int n;  
    int max=0;  
    while(scanf("%d",&n)&& n!=0)  
    {  
        a[n]++;  
    }  
  
    for(int i=1;i<10;i++)  
    {  
        if(a[i]>max)  
        {  
            max=a[i];  
        }  
    }  
    for(int i=1;i<10;i++)  
    {  
        if(a[i]==max)  
        {  
            printf("%d %d\n",i,a[i]);  
        }  
    }  
    return 0;  
}  