String
	cin.get() - 讀取下一個字元
	cin.peek() - 偷看即將要輸入的東西
	cin.getline() - 讀取整行 -cin.getline( 字元陣列, 最多字元數 )
	strlen(放入string名) - 得到字串長度
	strcmp(str1,s) - 比較字典順序
Vector (vector函式庫)
	初始化:vector <int> v
	v.push_back(1) - 加入1
	v2=v1 => v2(v1)
	push_back：把元素加到尾巴，必要時會進行記憶體配置
	pop_back：移除尾巴的元素
#define 識別名稱 代換標記
	Eg. #define MAX 32767
	Eg. #define IOU "I love you!"
使用const定義的變數，不能再重新設值：
sort(arr,arr+n)
reverse(str1.begin(),str1.end()) (string函式庫)
pointer 
	int *ptr
	ptr = &a

